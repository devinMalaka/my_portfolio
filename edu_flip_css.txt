
/* Education Flip Animation */
.edu-flip-container {
    perspective: 1000px;
    height: 3rem; /* Height of one degree line */
    margin: 0.5rem 0;
    display: flex;
    justify-content: center;
}

.edu-flipper {
    position: relative;
    width: 100%;
    height: 100%;
    transition: transform 0.6s;
    transform-style: preserve-3d;
    animation: degree-flip 6s infinite cubic-bezier(0.4, 0.2, 0.2, 1);
}

.edu-front, .edu-back {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
}

.edu-back {
    transform: rotateX(180deg);
}

/* Adjust edu-text to remove margins as container handles it */
.edu-text {
    margin: 0 !important;
    white-space: nowrap; /* Prevent wrapping for cleaner flip */
}

@keyframes degree-flip {
    0%, 45% {
        transform: rotateX(0deg);
    }
    50%, 95% {
        transform: rotateX(180deg);
    }
    100% {
        transform: rotateX(360deg);
    }
}
